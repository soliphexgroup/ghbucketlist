import { AdminComingSoon } from "@/components/dashboard/admin-coming-soon";

export default function AdminSettingsPage() {
  return (
    <AdminComingSoon
      title="Platform Settings"
      description="Site branding, commission rates, payment gateway keys, and SEO defaults will be configurable here once real integrations are wired up."
    />
  );
}
